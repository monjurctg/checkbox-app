import {StyleSheet, Text, View} from "react-native";
import React from "react";

const Rate = ({rate}) => {
  return (
    <View>
      <Text>Rate</Text>
    </View>
  );
};

export default Rate;

const styles = StyleSheet.create({});
